#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/C:/Users/86159/Desktop/vue3/server/node_modules/.store/acorn@2.7.0/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/C:/Users/86159/Desktop/vue3/server/node_modules/.store/acorn@2.7.0/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../acorn@2.7.0/node_modules/acorn/bin/acorn" "$@"
else
  exec node  "$basedir/../../../../../acorn@2.7.0/node_modules/acorn/bin/acorn" "$@"
fi
